`use strict`;

import {Terminal} from "./lib/terminal.core";
import {TerminalColors} from "./lib/terminal.colors";
import {TerminalDisplay} from "./lib/terminal.display";
import {TerminalKeyboard} from "./lib/terminal.keyboard";

export default Terminal;
export {
    Terminal,
    TerminalColors,
    TerminalDisplay,
    TerminalKeyboard
}